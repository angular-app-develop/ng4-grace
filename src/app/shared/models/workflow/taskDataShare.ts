export enum UserTaskApproved {
    managementApproved,
    hrApproved,
    modifyApproved
}

export enum WorkflowTaskDetailOperation {
    startevent1,
    todoTaskDetail,
    historyDetail
}

export enum UserTaskOperation {
    managementAudit,
    hrAudit,
    modifyApply
}